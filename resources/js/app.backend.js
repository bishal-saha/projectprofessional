import axios from 'axios';

window.Vue = require('vue');
window.axios = axios;

swal.setDefaults({
    buttonsStyling: false,
    confirmButtonClass: 'btn bg-indigo',
    cancelButtonClass: 'btn alpha-indigo'
});